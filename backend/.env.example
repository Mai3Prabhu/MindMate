# =============================================================================
# SUPABASE CONFIGURATION (REQUIRED)
# =============================================================================
# Your Supabase project URL - find this in your Supabase project settings
# Format: https://[project-ref].supabase.co
SUPABASE_URL=your_supabase_project_url_here

# Service role key for backend operations - find this in Supabase API settings
# WARNING: Keep this secret! Never commit to version control or expose to frontend
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# =============================================================================
# DATABASE CONFIGURATION (OPTIONAL)
# =============================================================================
# Direct PostgreSQL connection string - OPTIONAL for most use cases
# The Supabase client uses REST API by default, so this is only needed for:
# - Running database migrations with tools like Alembic
# - Executing raw SQL queries directly
# - Advanced database operations that bypass Supabase's API layer
#
# Find your database password in: Supabase Dashboard > Project Settings > Database
# Format: postgresql://postgres:[YOUR_PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres
# Example: postgresql://postgres:mySecurePass123@db.abcdefghijk.supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:[YOUR_PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres

# =============================================================================
# API KEYS (REQUIRED)
# =============================================================================
# Google Gemini API key for AI-powered features (therapy chat, emotion analysis, etc.)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# SECURITY KEYS (REQUIRED)
# =============================================================================
# AES-256 encryption key for encrypting sensitive data (journal entries, therapy sessions)
# Must be a 32-byte base64-encoded string
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_32_byte_base64_encryption_key_here

# JWT secret key for signing authentication tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your_jwt_secret_key_here

# =============================================================================
# APPLICATION SETTINGS (REQUIRED)
# =============================================================================
# Comma-separated list of allowed origins for CORS
# Add your frontend URLs here (development and production)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001